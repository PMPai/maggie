# Maggie 管理驾驶舱设计规范

> 本文是 Vibe Coding 的界面设计约束与验收标准。新增或重构仪表板时，先遵守本文，再写组件与样式；不得为了“看起来炫”而牺牲财务数据的可读性、可追溯性与操作效率。

## 1. 设计目标

为工程请款管理系统设计一个成熟、克制、信息密度高的中文管理驾驶舱。它应当让财务、项目经理与管理者在 10 秒内回答四个问题：

1. 目前签约、请款、开票、收款各是多少？
2. 哪些项目正在偏离计划或需要处理？
3. 未来几个月预期现金流与实际收款是否存在缺口？
4. 我点击后能直接进入哪个项目、报表或待办处理问题？

参考图的优点是：左侧信息表与右侧图表形成清晰分区、卡片不浮夸、数据密度高。保留这种“业务报表”的秩序感，但不要复制其单一蓝色、粗重表头和静态展示方式。

## 2. 视觉方向：专业业务系统，不是 AI 展示页

### 必须做到

- 中文优先：所有标题、图例、空状态、提示与操作文案使用自然、简短的中文，不堆砌营销化词汇。
- 以白色内容面、暖灰背景和细分隔线建立层级；卡片轻边框即可，不使用夸张悬浮阴影。
- 数据优先：金额使用等宽数字、右对齐；单位明确；表头固定语义，不用图标替代关键文字。
- 用“模块化经营看板”组织内容，而不是把每个指标做成彩色大卡片。
- 视觉留白有节制：桌面端内容宽度充分利用，避免大面积空白、超大标题或一屏只放三张卡片。
- 图表为决策服务。每张图必须有清晰标题、时间/项目范围、图例、悬停详情和可进入明细的路径。

### 明确禁止

- 禁止渐变背景、玻璃拟态、霓虹发光、漂浮 3D 图标、装饰性插画、彩色圆点墙、无意义的 AI 文案。
- 禁止“全蓝色企业后台”。蓝色只能作为主操作与计划/合同数据色，不可同时充当所有状态、所有图表序列和所有选中背景。
- 禁止彩虹配色、同一张图使用超过 3 个强调色、没有业务含义的饼图切片。
- 禁止静态指标卡、不可点击的图例、无响应的筛选器或只在 hover 才能看懂的核心数据。每一个可见控件和可行动数据都必须有明确反馈与去向。
- 禁止用红绿作为唯一信息通道；状态还要用文字、图标或形状区分。

## 3. 色彩、字型与数值规则

### 色彩系统：最多三种强调色

中性色（不计入强调色）用于背景、文字、边框：

| 用途 | 色值 | 说明 |
| --- | --- | --- |
| 页面底色 | `#F7F6F2` | 微暖灰，降低纯白眩光 |
| 内容面 | `#FFFFFF` | 卡片、表格、弹窗 |
| 主文字 | `#24303A` | 标题和关键数值 |
| 次文字 | `#66737F` | 标签、辅助说明 |
| 分隔线 | `#E6E8E7` | 表格与模块边界 |

全站仅使用以下三种强调色；允许使用它们的浅色背景与深浅层级，但不新增第四种强调色：

| 语义 | 主色 | 浅色背景 | 使用位置 |
| --- | --- | --- | --- |
| 靛蓝（主色） | `#355C9A` | `#EAF0FA` | 主按钮、当前导航、合同/计划、主要数据序列 |
| 琥珀（注意） | `#C88719` | `#FFF4DB` | 待处理、预警、预算/目标、次要数据序列 |
| 松绿（完成） | `#2F7D68` | `#E7F3EE` | 已完成、已收款、实际达成、正向状态 |

- 错误与高风险：沿用琥珀的深色 `#9C5D08`，并配合“异常/逾期”等文字和警示图标；不要额外引入亮红色。
- 同一图表最多 3 个业务序列：靛蓝 = 计划/合同，松绿 = 实际/完成，琥珀 = 差异/风险。
- 无数据、禁用、历史基线均使用中性灰，不占用强调色。

### 字体与数字

- 字体顺序：`"Noto Sans SC", "Microsoft YaHei", Inter, system-ui, sans-serif`。
- 页面标题 22–24px / 600；区块标题 15–16px / 600；正文 14px / 400；辅助文字 12px / 400。
- 金额和比例使用 `tabular-nums`。金额格式统一为 `NT$ 1,234,567` 或系统当前币别；超过一百万可在概览使用 `NT$ 123.5 万`，悬停或详情中必须显示完整金额。
- 百分比保留 1 位小数；未取得数据写 `—`，不能显示假零值。

## 4. 桌面端页面结构

桌面端采用固定左侧导航 + 可滚动主工作区。页面最小可用宽度为 1280px；主工作区最大宽度 1600px，左右内边距 28–32px。

```text
┌──────────── 左侧导航 248px ────────────┬──────────────────────── 主工作区 ────────────────────────┐
│ 品牌 / 系统名                          │ 面包屑（可选）                 全局筛选 / 刷新 / 导出     │
│                                        │ 页面标题 + 截止日期 + 数据更新时间                         │
│ 驾驶舱（当前）                          ├──────────────────────────────────────────────────────────┤
│ 项目管理                                │ 核心经营指标：4 个紧凑指标卡                              │
│ 文件收件箱                              ├───────────────────────────────┬──────────────────────────┤
│ 审批中心                                │ 资金进度与项目分布             │ 待办与风险清单             │
│ 我的请款 / 新建请款                     ├───────────────────────────────┴──────────────────────────┤
│ 报表中心                                │ 现金流趋势（主图）             │ 项目经营表（可排序/筛选）   │
│ 审计日志 / 管理                          ├──────────────────────────────────────────────────────────┤
│                                        │ 最近动态（可展开）                                      │
│ 用户信息 / 退出                         │                                                          │
└────────────────────────────────────────┴──────────────────────────────────────────────────────────┘
```

### 左侧导航

- 宽度 248px，固定于左侧；品牌区高度 64px，底部固定个人菜单。
- 菜单按工作流分组：`总览`、`业务处理`、`分析与审计`、`系统管理`。只有有权限的菜单才渲染。
- 当前项使用靛蓝浅底、靛蓝文字与左侧 3px 状态条；不要整块深色填满。
- 每项必须可点击，悬停、键盘聚焦、当前态均清晰可辨；收起后保留图标、Tooltip 和可访问名称。
- 未处理数量以小型琥珀徽章标在“审批中心/收件箱”右侧，点击进入已预筛选的清单。

### 顶部工作列

- 左侧：页面标题、简短说明或“统计截至 YYYY/MM/DD”。
- 右侧：项目范围、日期范围、刷新、导出。范围变化后显示“已更新”状态与更新时间。
- 筛选器应采用可清除的选择框；已生效条件在下方以可删除的筛选标签呈现。
- 刷新、导出、筛选都必须真的触发状态变化、请求或下载；加载时显示局部骨架屏/按钮忙碌态。

## 5. 仪表板功能与模块规范

默认顺序优先呈现“今天需要做什么”，再呈现“经营表现”。所有模块继承全局项目和日期筛选条件，并在标题附近显示自身统计范围。

### A. 核心经营指标（第一屏）

使用 4 张横向紧凑指标卡，不超过两行。每张卡包含：标签、主数值、同比/环比或相对合同的进度、一个极简进度条或迷你趋势、明确点击去向。

| 指标 | 主要含义 | 点击去向 |
| --- | --- | --- |
| 合同总额 | 当前范围内有效合同含税总额 | 项目汇总报表 |
| 累计批准请款 | 已入账请款金额及合同完成率 | 未开票/请款明细 |
| 已收款 | 已确认收款及开票回款率 | 收款差异报表 |
| 待处理事项 | 审批、变更、映射与超量异常总数 | 审批中心（带状态筛选） |

- 数值不因“看起来丰富”而添加随机涨跌箭头；只有真实比较期间存在时才显示变化。
- 待处理为 0 时用中性文字“当前无待处理事项”，不要使用绿色大面积庆祝状态。

### B. 资金进度与项目构成（第二屏左侧）

- 使用一张“合同—请款—开票—收款”阶段漏斗式水平进度图或分段条，而不是四个孤立圆环。
- 进度段可点击，进入对应明细；悬停显示金额、占合同额比例、计算口径。
- 项目构成只在项目数为 2–6 时使用环形图；项目超过 6 个时改为按合同额降序的横向条形图，并将其余项目合并为“其他”。
- 图例是筛选开关；隐藏某项目后，图表和表格同步更新，显示“已隐藏 n 项”和“重置”。

### C. 待办与风险清单（第二屏右侧）

按优先级展示 5–8 条，而非只给统计数字。每一行应有：状态符号、事项名称、项目名称、截止/逾期天数、责任动作和进入详情的箭头。

优先级从高到低：超合同异常 → 逾期审批/收款 → 待审核请款 → 待审核映射 → 一般变更。点击整行进入相应对象；“查看全部”进入带过滤条件的审批中心。

### D. 现金流趋势（主图）

- 使用组合图：松绿柱 = 实际收款，靛蓝线 = 预期收入，琥珀细线或阴影 = 差异/风险阈值（仅在有可靠规则时显示）。
- 默认展示最近 6 个月与未来 6 个月；历史/预测分界线要标清，预测数据不可伪装成实际数据。
- 可点击任一月份，打开右侧抽屉或进入报表，并将该月作为条件；tooltip 给出完整金额、同比/目标差异和数据来源。
- 坐标轴简洁，网格线极淡；图表禁止彩虹柱、立体效果、饼图替代趋势。

### E. 项目经营表（主操作区）

表格字段默认：项目编号、项目名称、状态、合同金额、累计请款、已开票、已收款、回款率、待处理事项。

- 可按金额、回款率、待办数排序；支持项目名/编号搜索、状态筛选和列显示设置。
- 项目编号和项目名称点击进入项目详情；金额点击进入对应已筛选明细；待办数点击进入审批队列。
- 数字右对齐，文字左对齐，状态使用文字标签；悬停行可见但不可干扰阅读。
- 支持分页或虚拟滚动；加载、空结果、权限不足、接口失败必须是不同状态，并提供可用的重试或清除筛选入口。

### F. 最近动态

显示近 5 条与当前权限相关的审计事件，包含动作、对象、操作者、相对时间与精确时间。点击进入对象或审计日志；“查看全部”保留当前筛选范围。

## 6. 交互规则（不可妥协）

1. **每一个有行动意义的元素都可交互。** 指标卡、图表柱/点/扇区、图例、表格链接、待办行、筛选标签、状态徽章都必须有明确点击结果或明确标注为只读。
2. **交互反馈不靠猜。** 可点击元素具备 `cursor-pointer`、hover、focus-visible、按下态；加载中禁用重复提交并显示进度。
3. **筛选统一传播。** 项目和日期是全局上下文；模块内点击产生的过滤条件应同步到 URL 查询参数，可复制、刷新、返回和分享。
4. **保持上下文。** 从卡片/图表进入详情后保留项目、日期、状态条件；返回时恢复滚动位置和筛选状态。
5. **钻取优先于弹窗。** 简单解释使用 tooltip；查看明细使用侧边抽屉；需要编辑或复杂审批时跳转到完整页面。
6. **权限即界面。** 无权限的数据/动作不要先显示再报错；看得见但不可操作的内容要说明原因并隐藏敏感金额。
7. **删除、作废、提交审批等高风险操作**必须二次确认，并展示对象、影响范围和不可逆说明。

## 7. 状态、空态与异常态

- **初次加载：** 使用贴近最终布局的骨架屏；禁止整页空白或永久旋转图标。
- **无数据：** 说明范围内没有什么数据，并提供“新建项目”“清除筛选”或“刷新”的下一步，不能使用插画占据主体。
- **接口失败：** 清楚说明“数据暂时无法载入”，保留最后成功数据并标注其时间；提供“重试”。
- **部分数据：** 例如现金流仅有预期没有实际，图例与说明明确标示“实际收款暂无记录”，不补零误导用户。
- **超量/逾期：** 使用琥珀警示图标、短文案、逾期天数与可执行操作，不能只靠颜色。

## 8. 响应式与可访问性

- ≥ 1280px：完整左侧导航、双栏分析区、4 列指标。
- 768–1279px：导航可收起为图标栏，分析区单栏或 2 列，指标 2 列。
- < 768px：左侧菜单进入抽屉；顶部筛选换行；卡片单列；表格改为项目摘要卡且保留关键操作。不得强行缩放桌面表格。
- 所有功能可用键盘访问；焦点环使用靛蓝且对比足够；图表提供键盘可达的数据点或对应数据表入口。
- 文本与背景对比度至少满足 WCAG AA；颜色不是传递状态的唯一方式。

## 9. 组件实现约束（Next.js + Tailwind + Recharts）

- 复用现有 `AppShell` 左侧导航、`Card`、`StatCard`、`FilterBar`、`ErrorBanner` 与金额格式化逻辑；先扩展通用组件，再避免在页面内复制样式。
- 将主题色定义为语义 token，例如 `--color-primary`、`--color-attention`、`--color-success`，图表与按钮都引用 token，禁止散落硬编码色值。
- Recharts 的 `Tooltip` 使用中文格式化：金额、日期、百分比和数据口径一致；图例可点击；空数组不渲染空框图。
- 所有 API 状态都要处理：loading、success、empty、error；请求需要可取消或避免组件卸载后更新状态。
- 核心接口优先使用现有的 `/dashboard/summary`、`/dashboard/cash-flow`、`/dashboard/payment-trend` 与项目列表；新增接口必须说明权限范围、聚合口径和空值行为。

## 10. Vibe Coding 执行提示

实现仪表板时，按以下要求生成代码：

```text
你正在为 Maggie 工程请款系统实现中文“管理驾驶舱”。严格遵循仓库根目录 Design.md。

目标：做一个成熟的业务运营界面，不是营销落地页，也不是炫技数据可视化。保留现有左侧菜单与权限逻辑；使用暖灰底、白色内容面、靛蓝 #355C9A、琥珀 #C88719、松绿 #2F7D68 三种强调色。不能新增第四种强调色，不能使用渐变、玻璃拟态、彩虹图表或大面积深色背景。

页面按“顶部范围筛选 → 4 个可钻取经营指标 → 资金进度/项目构成 + 待办风险 → 现金流趋势 → 可排序项目经营表 → 最近动态”组织。每个指标卡、图表数据点、图例、待办行与表格业务字段都要有可用点击行为；所有筛选应反映到 URL，并在钻取页面保留上下文。

请复用既有组件与真实接口，补齐 loading、empty、error、权限和键盘焦点状态。先实现数据口径正确和交互闭环，再优化视觉。完成后逐项自检本文件第 11 节。
```

## 11. 完成前验收清单

- [ ] 左侧功能菜单固定在页面左侧，按权限过滤，当前项、悬停态、焦点态清晰。
- [ ] 页面没有渐变、玻璃拟态、彩虹色或单一蓝色主导；强调色只有靛蓝、琥珀、松绿三种。
- [ ] 所有金额、百分比、日期、状态与图例均为中文且格式一致。
- [ ] 四个核心指标有真实口径、加载态、点击去向；没有伪造同比/趋势。
- [ ] 图表可悬停、可钻取、图例可筛选；不超过三种业务强调色；无数据时不渲染误导性图形。
- [ ] 表格可排序/筛选/搜索，项目与金额可进入相应详情，数字右对齐。
- [ ] 待办与风险可直接进入处理页，优先级和截止/逾期信息明确。
- [ ] 筛选、刷新、导出、按钮、状态徽章都有真实行为与加载反馈。
- [ ] 页面在桌面、平板、手机均可用；键盘导航、焦点和非颜色状态提示可用。
- [ ] 视觉审阅时，界面更像长期迭代的财务运营系统，而不是一次性 AI 生成模板。
